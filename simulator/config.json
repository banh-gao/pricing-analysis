{
    "simulation" : {
        // seed(s) to initialize PRNGs
        "seed" : [0, 1],

        // resource to allocate
        "resource" : "memory",
        // scale for the resources quantities specified in configuration (MebiBytes)
        "resource_scale" : 2**10,
        // resource size distribution in megaBytes
        "size" : [
            {"distribution" : "pareto" , "shape" : 1, "mode" : 4000, "int" : 1}
        ],
        // Run simulation as long as the probability of allocating resources is
        // higher than this value
        "halting_threshold": 0.05,

        // offer value for per single megaByte
        "offer" : {"distribution" : "const", "mean" : 1},

        // application to deploy
        "application" : "alpine",
        // API endpoint
        "endpoint" : "http:\/\/localhost:8080\/api",
        // log file name using configuration parameters
        "output" : "output_{size.mode}_{seed}.csv"
    }
}
